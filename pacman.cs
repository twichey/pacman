using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace pacman
{
    class pacman
    {
        public Pacman:sprite
        {
            public pacman(
            vector2 location,
            texture2D texture,
            rectangle initialframe,
            vector2 veloocity):
                base(location,texture,initialframe, velocity)
            {
            }

        public override update(Gametimem gametime)
        {
    }
}
